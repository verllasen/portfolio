services:
  - type: web
    name: corso-family-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m bot.main
    healthCheckPath: /health
    envVars:
      - key: DISCORD_TOKEN
        sync: false
