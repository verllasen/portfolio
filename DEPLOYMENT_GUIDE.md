# 🚀 Как бесплатно запустить сервер ITeam Tycoon в интернете

Чтобы играть с друзьями через интернет, серверная часть должна работать в облаке. Мы будем использовать сервис **Render.com**, так как он бесплатен и поддерживает Node.js.

---

## Шаг 1: Подготовка кода (GitHub)

1.  У вас должен быть аккаунт на [GitHub](https://github.com/).
2.  Создайте **новый репозиторий** (New Repository) на GitHub. Назовите его, например, `iteam-server`.
3.  Загрузите файлы из папки `server` в этот репозиторий.
    *   *Важно:* В корне репозитория должны лежать файлы `package.json` и `index.js`.
    *   Если вы загружаете весь проект целиком, убедитесь, что вы знаете, в какой папке лежит сервер.

**Пример структуры репозитория (вариант "Только сервер"):**
```text
iteam-server/
├── package.json
├── index.js
└── ...
```

---

## Шаг 2: Создание сервера на Render

1.  Зарегистрируйтесь на [Render.com](https://render.com/).
2.  Нажмите кнопку **New +** и выберите **Web Service**.
3.  Подключите свой GitHub аккаунт и выберите репозиторий `iteam-server`, который вы создали.
4.  Заполните настройки:
    *   **Name**: `iteam-game-server` (любое имя)
    *   **Region**: Frankfurt (ближе к РФ) или любой другой.
    *   **Branch**: `main` (или `master`).
    *   **Root Directory**: Оставьте пустым (если файлы в корне) или напишите `server` (если загрузили весь проект).
    *   **Runtime**: Node
    *   **Build Command**: `npm install`
    *   **Start Command**: `node index.js`
5.  Выберите тариф **Free** (Бесплатно).
6.  Нажмите **Create Web Service**.

---

## Шаг 3: Получение ссылки

Render начнет сборку и запуск сервера. Это займет 1-2 минуты.
Когда процесс завершится, вы увидите надпись **Live** или зеленую галочку.

Вверху страницы под названием вашего сервиса будет ссылка.
Она выглядит так: `https://iteam-game-server.onrender.com`

**Скопируйте эту ссылку!** Это адрес вашего игрового сервера.

---

## Шаг 4: Настройка игры (Клиента)

Теперь нужно научить игру подключаться к этому новому адресу вместо `localhost`.

### Способ А: Без пересборки (Быстро)
Этот способ подходит, если вы уже скинули `.exe` друзьям.

1.  Запустите игру.
2.  Нажмите `Ctrl + Shift + I` (или F12), чтобы открыть консоль разработчика.
3.  Перейдите во вкладку **Console**.
4.  Вставьте команду (заменив ссылку на вашу):
    ```javascript
    localStorage.setItem('server_url', 'https://ваш-проект.onrender.com');
    window.location.reload();
    ```
5.  Игра перезагрузится и подключится к облаку. Это нужно сделать **каждому** игроку один раз.

### Способ Б: Вшить адрес в игру (Надежно)
Если вы хотите сделать "чистый" установщик.

1.  Откройте файл `client/src/store/useGameStore.ts`.
2.  Найдите строку:
    ```typescript
    const SERVER_URL = localStorage.getItem('server_url') || 'http://localhost:3000';
    ```
3.  Замените `'http://localhost:3000'` на вашу ссылку с Render:
    ```typescript
    const SERVER_URL = localStorage.getItem('server_url') || 'https://ваш-проект.onrender.com';
    ```
4.  Заново соберите проект командой:
    ```bash
    npm run dist
    ```
5.  Отправьте новый `.exe` файл друзьям.

---

## ⚠️ Важное примечание про бесплатный тариф

На бесплатном тарифе Render сервер **"засыпает"** (выключается), если никто не пользуется им 15 минут.
*   **Первое подключение** после простоя может занимать **до 50 секунд** (пока сервер просыпается).
*   Просто подождите минуту, если игра не подключается сразу.

---

## 🎉 Готово!

Теперь вы можете играть с друзьями, даже если находитесь в разных городах.
1. Скиньте другу `.exe` файл.
2. Сообщите ему свой **Код Друга**.
3. Создайте студию и покоряйте IT-мир!
