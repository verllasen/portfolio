# 📘 Документация ITeam Tycoon

## 🎮 Об игре
**ITeam Tycoon** — это многопользовательский симулятор IT-студии. Игроки объединяются в команды, распределяют роли (Frontend, Backend, DB и т.д.) и выполняют контракты, написав реальный (или псевдо) код.

### Основные механики
1.  **Студия**: Игрок может создать свою студию или вступить в существующую по **Коду Доступа**.
2.  **Роли**: В каждой миссии участники выбирают роли:
    *   🛠 **HTML/CSS**: Верстка интерфейсов.
    *   ⚙️ **JavaScript**: Логика приложения.
    *   🗄 **SQL**: Базы данных.
3.  **Миссии**: Команда берет контракт, выполняет задачи в реальном времени.
4.  **Редактор**: Встроенный редактор кода (на базе Monaco Editor) с подсветкой синтаксиса.
5.  **Экономика**: За выполнение миссий студия получает деньги и рейтинг, а игроки — опыт (XP).

---

## 🛠 Технический стек

### Клиент (Client)
*   **Фреймворк**: React + Vite
*   **Язык**: TypeScript
*   **Стили**: Tailwind CSS
*   **Стейт-менеджмент**: Zustand (с персистенцией данных)
*   **Десктопная оболочка**: Electron (через electron-builder)
*   **Редактор кода**: @monaco-editor/react
*   **Связь с сервером**: Socket.IO Client

### Сервер (Server)
*   **Платформа**: Node.js
*   **Фреймворк**: Express
*   **Real-time**: Socket.IO
*   **База данных**: In-memory (хранится в оперативной памяти сервера для скорости разработки)

---

## 📂 Структура проекта

```text
ITeam/
├── client/                 # Клиентская часть (React + Electron)
│   ├── electron/           # Настройки Electron (main process)
│   ├── src/
│   │   ├── components/     # Переиспользуемые UI компоненты
│   │   ├── pages/          # Страницы (Lobby, Workspace, Menu)
│   │   ├── store/          # Глобальное состояние (useGameStore)
│   │   └── types/          # TypeScript интерфейсы
│   └── package.json
│
└── server/                 # Серверная часть
    ├── index.js            # Точка входа сервера
    └── package.json
```

---

## 🚀 Запуск для разработки

### 1. Запуск Сервера
```bash
cd server
npm install
npm start
# Сервер запустится на порту 3000
```

### 2. Запуск Клиента (Режим разработки)
```bash
cd client
npm install
npm run dev:electron
# Запустится окно Electron с Hot Module Replacement
```

---

## 📦 Сборка (Release)

Для создания `.exe` файла установщика:

1.  Убедитесь, что сервер настроен (локально или удаленно).
2.  Выполните в папке `client`:
    ```bash
    npm run dist
    ```
3.  Готовый файл будет в папке: `client/release/ITeam Launcher Setup X.X.X.exe`

---

## 🌐 Сетевое взаимодействие

Клиент подключается к серверу через WebSocket.
Адрес сервера по умолчанию: `http://localhost:3000`.

**Изменение адреса сервера:**
Адрес можно изменить динамически без пересборки приложения, выполнив в консоли разработчика (Ctrl+Shift+I) команду:
```javascript
localStorage.setItem('server_url', 'https://ваш-сервер.com');
window.location.reload();
```
